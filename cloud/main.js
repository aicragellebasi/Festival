require('cloud/app.js');

/*
var Activity = Parse.Object.extend('Activity');
Parse.Cloud.beforeSave(Activity, function(req, res){
	var activity = req.object;
	//
	activity.set('isFull',isFull);

	res.success();
});
*/
